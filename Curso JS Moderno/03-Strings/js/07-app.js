const producto = 'Monitor de 20 pulgadas';
console.log(producto.toUpperCase()); //todo a mayuscula

console.log(producto.toLowerCase()); //todo a minuscula

const email = "CORREO@GMAIL.COM";
console.log(email.toLowerCase());

// cambiar de nuemro a string
const precio = 300;
console.log(precio);
console.log(precio.toString()); //.toString